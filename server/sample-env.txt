# =============================================
# ENVIRONMENT CONFIGURATION FILE
# =============================================
# 
# INSTRUCTIONS:
# 1. Rename this file to ".env" (remove "sample-" and ".txt")
# 2. Update the values as needed for your environment
# 3. Make sure .env is in your .gitignore file
#
# =============================================

# Server Configuration
PORT=5000

# MongoDB Connection String (using 127.0.0.1 to force IPv4)
MONGODB_URI=mongodb+srv://shailja2005limbu_db_user:Strongpassword1!@we-care.jixzsjt.mongodb.net/We-Care?appName=We-Care

# JWT Secret (IMPORTANT: Change this in production!)
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# JWT Expiration (optional, defaults to 7d in code)
# JWT_EXPIRES_IN=7d

# Node Environment (development | production)
NODE_ENV=development

# Gemini API Key (backend only - never expose this in frontend)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional model override
GEMINI_MODEL=gemini-2.5-flash

# =============================================
# EMAIL CONFIGURATION (GMAIL SMTP)
# =============================================
# Use a Gmail App Password for EMAIL_PASS.
# Do not use your regular Gmail account password.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password
EMAIL_FROM_NAME=We Care Skincare
# Set to true only in development if your network/proxy injects self-signed TLS certificates.
EMAIL_ALLOW_SELF_SIGNED=false

# Frontend URL used in email links
FRONTEND_URL=http://localhost:3000

# eSewa Sandbox (v2)
ESEWA_MERCHANT_ID=EPAYTEST
# Sandbox secret key from eSewa test credentials.
ESEWA_SECRET_KEY=8gBm/:&EnhH.1/q
ESEWA_PAYMENT_URL=https://rc-epay.esewa.com.np/api/epay/main/v2/form
ESEWA_VERIFY_URL=https://rc.esewa.com.np/api/epay/transaction/status/
